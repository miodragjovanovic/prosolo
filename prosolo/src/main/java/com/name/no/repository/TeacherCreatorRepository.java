package com.name.no.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.name.no.model.TeacherCreator;

public interface TeacherCreatorRepository extends JpaRepository<TeacherCreator, String> {

}
